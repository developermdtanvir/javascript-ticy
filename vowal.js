// write an function ths function remove word vowel word remove
/*
Write a function that takes a string as an argument and returns a new string with all the vowels removed. Assume that the input string contains only lowercase letters.

Here's an example:

Input: "hello world"
Output: "hll wrld"

Can you give it a try?

*/
const vowels = ["a", "e", "i", "o", "u"];

function removeVowels(letter) {
  const splitLetters = letter.split(" ");
  for (let i = 0; i < splitLetters.length; i++) {
    let splitLetter = splitLetters[i];
  }
}

removeVowels("Kamal uddin");
removeVowels("MD Tanvir Hossain");
